# AntiCheat Monitor Plugin Configuration

# WebSocket server URI (Node.js backend)
ws-uri: "ws://localhost:55211/spigot"

# Shared secret token. Must match ACS_AUTH_SECRET when backend auth is enabled.
auth-token: ""

# Server display name
server-name: "main-server"

# Movement sampling interval (milliseconds)
sample-interval: 250
